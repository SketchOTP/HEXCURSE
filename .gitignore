# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log

# Dependency directories
node_modules/

# Environment variables
.env

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS specific
.DS_Store

# Skills CLI default install path (skills add …); canonical copy lives under .cursor/skills/ when mirrored
.agents/

# Cursor hooks local state
.cursor/hooks/state/

# Machine-specific path to cursor-governance setup.js (seeded by --doctor on source repo)
.cursor/hexcurse-installer.path

# Repomix output (can be large; may mirror repo contents)
repomix-output.xml

# Cached agent prompt from --parse-prd-via-agent (local only)
.taskmaster/agent-parse-prompt.txt

# Local clones for one-off analysis (not part of HexCurse)
.scratch-claw-code/
.scratch-*/

# Serena local overrides (machine-specific; keep out of git)
.serena/project.local.yml